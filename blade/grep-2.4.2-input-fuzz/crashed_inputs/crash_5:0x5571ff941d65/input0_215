Before an alternative
store_op1 (op, loc, arg32768, arg2)
store_op1 (op, loc, arg9223372036854775810, arg3)
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
        : next_bac void
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARE? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
        : next_backslash && next_next && *next_next == ')')
Before an alternative
include
struct trie
 