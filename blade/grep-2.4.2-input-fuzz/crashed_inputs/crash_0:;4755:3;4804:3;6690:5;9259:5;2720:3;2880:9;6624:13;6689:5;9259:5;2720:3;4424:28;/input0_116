Before an alternative
store_op0 (op, loc, arg1, arg1)
sto󠀩re_op0 (op, loc, arg1, arg-255)
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
        : next_backslash && next_next && *next_next == ')')
Before an alternative
include
struct trie
 