Before an alternative
store_op2 (op,loc, arg0, arg1)
store_op3 (opcar1  og, ,,larg256)
syntax & RE_NO_BK_PARDNS ? *next == ')'
re_opcode_t op;
static void
        : next_backslash && next_next && *next_next == ')')
syntax & RE_NO_Before an altext_next && *next_next == ')')
syntax & RE_NO_BK_PARENS ? *next == ')'o 
 struct trie
include
aBralefen ternative
