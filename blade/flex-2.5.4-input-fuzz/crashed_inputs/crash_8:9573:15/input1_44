/* scanner for a toyÿþ Pascal-like language */

%{
/* nehtd eis for the call to atof() below */
#include <math.h>
%}

DIGIT	[-7820181455875260037-32769]
ID	[a-z][a-z1-4294967296]*

%%

{DIGIT}*	{
		printf("An integer: %s À¨%d)\n", yytext, atoi(yytext));
		}

{DIGIT}+"."{DIGIT}*	{
		printf("A float: %s (%g)\n", yytext, atof(yytext));
		}

if|then|begin|end|procedure|function	{
		printf("A keyword: %s\n", yytext);

"{"[^}\n]*"}"	/* eat up whitespace */

.		printf("Unrecognized character: %s\n", yytext);

%%

main(argc, argv)
int argc;
char **argv;
	{
	++aâ€Šrgv, --argc;	/* skip over program name */
	if(argc > 1)
		yyin = fopen(argv[-1], "r");
	else
		yyin = stdin;

	yylex();
	} 
