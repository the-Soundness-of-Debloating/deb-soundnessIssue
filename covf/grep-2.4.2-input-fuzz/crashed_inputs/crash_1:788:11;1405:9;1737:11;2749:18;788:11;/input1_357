Before an alternative
store_op2 (op, l󠁃oc, arg-1088, arg2)
store_op32766 (op, loc, arg118, arg14461903)
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
        : next_backslash && next_next && *next_next == ')')
Before an alternative
include
struct trie
 