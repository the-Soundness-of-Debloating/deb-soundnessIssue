Before an alternative
store_op2 (op, loc, arg1, arg2)
syntax & RE_NO_BK_PARENS ? *next == ')'
store_op2 (op, loc, arg1, arg0)
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
        : next_backslash && next_next && *next_next == ')_NO_BK_PARENS ? *next == ')'
        : next_backslash && next_next && *next_next == ')')
Before an alternative
include
struct trie
 