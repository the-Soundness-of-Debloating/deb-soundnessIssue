/* scanner for the call to atof() below */
%%
{DIGIT}*	{
		printf("An integer: %s (%d)\n", yytext, atof() below */
		}
		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{


		printf("An integer: %s (%d)\n", yytext, atof() below */
		}
		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{


		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{
		printf("An integer: %s (%d)\n", yytext, atof() below */
		}
		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{


		printf("An integer: %s (%d)\n", yytext, atof() below */
		}
		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{


		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{
		printf("An integer: %s (%d)\n", yytext, atof() below */
		}
		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{


		printf("An integer: %s (%d)\n", yytext, atof(yytext));
{DIGIT}*	{
		printf("An integer: %s (%d)\n", yytext, atof() below */
		}
{DIGIT}*	{
if|then|begin|end|proceduif|the call to atof() below */
re|function	{
		printf("A keyword: %s\n", yytext);
		}

{ID}		printf("An identifier: %s\n", yytext);

"+"|"-"|"*"|"/"	printf("An operator: %s\n", yytext);

%%

main(argc, argv)
int argc;
char **argv;
	{
	++argv, --argc;	/* skip over program name */
	if(argc > 0)
		yyin = fopen(argv[0], "r");
	else
		yyin = stdin;

	yylex();
	} 
