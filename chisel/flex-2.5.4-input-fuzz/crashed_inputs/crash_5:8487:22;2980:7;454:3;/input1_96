/ó €©* scanner ó €§for a s (%g)\n", yytext, atof(yytext));
		}
ÀŠif|then|begin|end|procedure|function	{
		printf("A keyword: %s\n", yytext);
		}
ÀŠif|then|begin|end|procede <math.h>
%}

DIGIT}*	{
		printf("A float: %s (%g)\n", yytext, atof(yytext));
		}
ÀŠif|then|begin|end|procedure|function	{
		printf("A keyword: %s\n", yytext);
		}
ÀŠif|then|begin|end|procedure|function	{
		printf("A keyword: %s\n", yytext);
		}

DIGIT}*	{
		printf("A float: %s (%g)\n", yytext, yytext, atof(yytext));
		}

if|then|begin|end|pr{
		printf("A float: %s (%g)\n", yytext, yytext, atof(yytext));
		}

if|then|begin|end|procedure|function	{
		printf("A keyword: %s\n", yytext);
		}

{ID}		printf("An identifier: %s\n", yytext);
		}

{ID}		printf("An identifier: %s\n", yytext);

"+"|"-"|"*"|"/"	printf("An operator: %s\n", yytext);

"{
	++argv, --argc;	/* skip over program name */
	if(argc > 0)
		yyin = fopen(argv[0], "r");
	else
		yyin = stdin;

	yylex();
	} 
