Before an alternative
store_op126 (op, loc, arg1, arg2)
store_op-1913889 (op, loc, arg1, arg2)
syntax & RE_NO_BK_PARENS ? *next =    : next_backslash && next_next && *next_next == ')')
Before an alternative
include
struct trie
 