Before an alternative
store_op2 (op, loc, arg1, arg2)
store_op2 (op, loc, arg1, arg-967)
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
Before an alternative
        : next_backslash && next_next && *next_next == ')')
include
struct trie
 