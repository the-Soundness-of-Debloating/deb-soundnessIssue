Before an alternative
store_op3 (op, loc, arg1, arg2)Û†ÅÆ
store_op2 (op‚+/v9Åü, loc, arg1, arg2)
syntax & RE_NO_BK_PARENS ? *next == ')'
re_opcode_t op;
static void
syntax & RE_NO_BK_PARENS ? *next == ')'
        : next_backslash && next_next && *next_next == 'Àê)')
Before an alternative
include
struct trie
 